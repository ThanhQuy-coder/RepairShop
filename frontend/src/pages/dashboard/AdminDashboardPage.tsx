import StaffDashboardPage from './StaffDashboardPage';

// Admin xem đúng summary giống Receptionist (toàn bộ ticket hệ thống) — chỉ khác Sidebar/menu
// (đã tách riêng ở AdminLayout, Task 5.3), nội dung Dashboard không cần viết lại.
export default function AdminDashboardPage() {
  return <StaffDashboardPage />;
}
